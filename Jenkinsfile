pipeline {
    agent any 
    stages {
        stage ('compile job for my code) {
               steps {
                   checkout scm 
                   sh 'mvn compile'
               }
               }
               }
               }
               
