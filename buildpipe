pipeline {
  agent any
  stages {
    stage('checkout the code') {
      steps {
        checout scm
      }
    }
    stage ("build my code") {
      steps {
        sh "mvn build"
      }
    }
  }
}
