pipeline {
  agent any
  stages {
    stage('checkout the code') {
      steps {
        checkout scm
      }
    }
    stage ("build my code") {
      steps {
        sh "mvn compile"
      }
    }
  }
}
