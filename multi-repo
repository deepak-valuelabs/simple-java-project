pipeline {
    agent any
    stages {
        stage(checkout) {
            steps {
                dir('consul') {
                    git url: "https://github.com/Dpz94/Sample-Pro.git"
                }
        }
    }
}
}
